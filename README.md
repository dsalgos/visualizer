# Visualizer

This is a standalone C++ application that can be used to visualize common data structures and algorithms, using the `dsalgos` project.
It depends upon the `dsalgos` library: the `dsalgos` library provides a static library which this program can link against.

Apart from the `dsalgos` library, it also depends upon the `FTXUI` library for building a command line interface for the application.
